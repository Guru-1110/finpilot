# FinPilot — secrets template.
#
# Copy this file to ".streamlit/secrets.toml" (which is gitignored) and paste your
# own key to enable AI narration. WITHOUT a key the app still works fully — the
# "Explain my plan" button falls back to a deterministic plain-English summary.
#
# Get a free Gemini API key at: https://aistudio.google.com/app/apikey
# Never commit your real key.

GEMINI_API_KEY = "your-gemini-api-key"
